import * as React from 'react';
import PageComponent from '../components/page/page';

export default function PageWhitelist() {
    return (
        <PageComponent navigation pageNumber={3}>
            Whitelist
        </PageComponent>
    );
}